# ENG-002 施工紀錄

對應 [SDD-002](../sdd/002-lesson-preparation.md)。日期 2026-09-22。

## N1 規格與盤點（完成）

- 沿用 ENG-001 未提交工作；附件及提案保留。未授權本輪發布，因此本地施工，不 commit／push／deploy。
- 依提案最小版先做每日故事、外帶午餐與餐點售完變化課，住宿／邀約另案。
- 純靜態 PWA；現有 quiz 固定順序、無作答保存；原角色扮演是照稿，不是語意任務評量。
- 新內建課須保持四組核心音檔要求，不用 `preGeneratedAudio:false` 規避檢查。暖身詞與任務變化句另用裝置語音並明示。
- 規格先定，再做可選 learning metadata、暖身及任務入口。舊課不受影響。

## N2 資料與驗證（初版完成）

- 新增三課可選 learning metadata、完整詞界與句 ID 驗證、內容版本、分開的字詞／理解／任務結果。
- 新增 12 項單元測試；包含隨機選項原索引、詞形不誤撞子字串、曾看提示不得標獨立完成。
- 新增原子 kvUpdate，避免不同頁面同時修改學習紀錄時互相覆蓋。

## N3 UI 與儲存（本機驗收完成）

- 新增可跳過的 prepare／task 路由。字詞揭示前不放入 DOM；裝置語音未成功時不能選「先聽就懂」。
- 已編輯課全文預設收起；新課三題中文理解題答後才顯示解釋與證據句。角色任務是固定自練，不冒稱 AI。
- 匯出新增 lessonLearningProgress。現有無還原功能，本輪仍不宣稱具備。
- 課程詳情增加 route token 保護，避免新增非同步讀取後舊頁把新頁蓋掉；新頁保存途中失敗不跳項，仍可重試或跳過。
- 測試時發現舊結束頁會對全程跳過的情況說「這課完成了」並聲稱加入複習；改為「本輪練習結束」與誠實的 SRS 說明。
- 精聽全域空白鍵原本可能攔截測驗按鈕；排除按鈕／連結等互動元素，實測 Space 選答案成功。

## N4 音檔與驗收（可執行範圍完成；Edge 待授權）

- 本機 Kokoro 使用離線模型（HF_HUB_OFFLINE／TRANSFORMERS_OFFLINE），產生美式／英式各 12 段，共 24 段。
- Edge 外部語音傳輸被安全審核拒絕：原請求未具體同意將未發布文字傳給 Microsoft。已向使用者詢問，只待同意後才做；未改用繞路方式。
- 初次 check：56 個 JS 模組語法、56 項 Node 測試通過；內容驗證正確擋下新課缺少 edge-us／edge-gb，不降低完整性標準。
- 本機正常更新流程實測：v2026.09.22.1 顯示 v2026.09.22.2 就緒提示，按更新後看見預習入口與收起的全文，未清除資料。
- 最後修正再升為 v2026.09.22.3，實測 `.2 → .3` 提示及切換；沒有沿用同版號快取假裝載入新程式。
- 最終語法 57 個模組、56 項 Node 測試全過；`npm run check` 的內容階段只有兩項失敗：l1-07 缺 edge-us／edge-gb。不宣稱整體 check 成功。
- 24 段新 Kokoro MP3 逐檔 ffprobe 檢查可解析且時間 > 0；美式總長 17.8 秒、英式 20.2 秒。不等於逐句人工發音品評完成。
- `tools/qa/learning.html`：5 項全部通過，含 20 筆同時更新、reducer 錯誤回滾、不同學習紀錄合併、匯出形狀 JSON 往返、僅刪本輪暫存鍵。未下載實際設定頁匯出檔。
- 手動完整走每日核心 6 詞：第一項先聽需提示，其餘直接看字；重開從剩餘 5 項續練，完成摘要 0 聽熟／5 看字理解／1 仍需練習，沒有誤報全部聽懂。補充 inspected 顯示 inspect 原形，跳過可進入精聽。
- 舊課 l1-02 無新 learning 欄位，仍顯示原先全文、查詞與練習入口；l1-05 可接到新 l1-07。
- 任務預設無完整示例，顯示句架後再切回無提示，結束仍禁止「獨立完成」；選「看提示完成」後可在課程詳情看到自評。
- 新課測驗實測選錯第 1 題、答對後兩題，回課程顯示 2/3；答案改位置仍判對，作答前沒有解釋與證據句，作答後才顯示。
- 390 × 844 暖身截圖與任務 DOM 尺寸檢查無水平溢出，最終還原視窗尺寸。裝置英語語音至少一次正常完成；未做真機／飛航模式與全音檔聽感驗收。
- 本機預覽留下本輪 UI 測試的 6 項每日暖身自評、新課 2/3 測驗與「看提示完成」自評，這些是 QA 結果，不是使用者學習成效。沒有改動正式站資料；SRS 未因跳過而新增掌握紀錄。

## N5 初次交付與限制（當時待音檔，後由 N6 補齊）

- SDD、程式入口、測試、know-how 與工程索引互相連結；原提案維持歷史設計稿，不改成當時已實作。
- 內容索引目前 109 課／1852 句；增加 1 課 12 句。既有兩課只加 learning metadata，不修改原英文、中文或原音檔。
- 未 commit／push／deploy；PDF 不讀入新課、不提交、不部署。商用審查暫緩。
- 當時剩餘阻礙是 Edge 外部語音傳輸需具體同意。兩組語音未齊前，不可走正式發布；後續處理見 N6。
- 住宿 B、邀約 C、全課庫選詞、自由回應 AI、備份還原、客觀能力評量仍未施工。

## N6 授權後補齊音檔（完成，未發布）

- 使用者對具體傳輸範圍回覆「ok」：僅新課 12 句原創英文送至 Microsoft Edge 語音服務，產生美式／英式各 12 段。
- 執行前核對缺檔清單只含 `l1-07`；不強制重製既有課、不傳附件或學習紀錄。本輪仍不 commit／push／deploy。
- 執行前的缺檔守衛確認恰好 24 段、全屬 `l1-07`，才啟動既有產音器；美式／英式各 12 段產生成功。工具雖掃描全課庫，僅缺檔會產生外部請求，沒有強制重製其他課文。
- 新課四組音檔共 48 段，逐檔 ffprobe 可解析且長度 > 0。新 Edge 美式總長 43.80 秒、英式 39.98 秒；原 Kokoro 音檔不重製，既有課 manifest 項目與 HEAD 完全一致。
- 重新執行完整 `npm run check` 成功：57 個 JS 模組語法、56 項 Node 測試、109 課／1852 句／11 組語音／12428 個音檔內容驗證全過。
- 本機 HTTP 檢查確認新課語音索引與四組 48 段 MP3 均成功供檔，下載位元組與磁碟內容一致。初次連線被沙箱限制擋下，取得本機連線權限後通過；無外部資料傳輸。
- 瀏覽器重新載入新課與播放器正常；語音選單已顯示美式 Edge 可用，未變更使用者語音設定。瀏覽器沒有警告／錯誤；本輪不再寫入測驗或任務自評。
- 本輪僅新增媒體、更新 manifest 與文件；不改 app shell，保留本地版號 `2026.09.22.3`。content 採 network-first，重新載入取得新索引，不需清除學習資料。
- 不將檔案可解析等同人工發音品質驗收。真機、斷網重開、全音檔人工聽評與正式站升級仍未測；商用稽核仍暫緩。

## 需求驗收對照

| ID | 已驗證 | 未測／限制 |
|---|---|---|
| LP-01 | 三課詳情入口、l1-05 接 l1-07、l1-02 舊課不變、跳過可精聽 | 非全課庫 |
| LP-02 | 先聽 DOM 無字詞；未播 disabled；揭示與兩段自評 | 未模擬所有無聲裝置；裝置語音依環境 |
| LP-03 | 3 課內容驗證、詞界測試、inspected UI；6 核心／2 補充 | 無 AI 自動選詞、無 CEFR 認證 |
| LP-04 | 重開接剩餘、核心摘要、Node 重練與改版；資料庫原子測試 | 未以兩個真實瀏覽器同時寫入；同一 IndexedDB 20 筆併發已測 |
| LP-05 | 打散後錯／對判分、答後解釋、2/3 保存、Space 選項 | 測驗僅保存最近一輪完整結果；中途離開不保留未完成作答 |
| LP-06 | 三層提示與隱藏示例；有提示禁止記獨立；自評保存 | 不會聽懂／評分學生實際說話；固定搭檔台詞 |
| LP-07 | 暖身不傳整句 ID；授權後四組 48 段可解析、manifest 齊全、本機 HTTP 位元組相符；既有課不重製 | 未全音檔人工聽評；裝置語音受環境影響 |
| LP-08 | kv 原子寫入／回滾、匯出欄位＋JSON形狀往返、SRS 分離 | 沒有下載實檔驗證與還原功能 |
| LP-09 | 57 模組語法、56 單測、5 DB 瀏覽器測試、390px、正常版本升級、diff check；N6 完整 check／內容驗證通過 | 真機離線與正式站未驗收；瀏覽器互動驗收的完整範圍見 N4 |
